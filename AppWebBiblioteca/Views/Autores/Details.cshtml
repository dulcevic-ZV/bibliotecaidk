@model AppWebBiblioteca.Models.Autor;

@{
    ViewData["Title"] = "Detalles del autor";
}

<div class="card shadow-sm">
    <div class="card-body">
        <h2 class="card-title">@Model.Nombre</h2>
        <h6 class="card-subtitle mb-3 text-muted">@Model.Nacionalidad</h6>
    </div>
    <dl class="row">
        <dt class="col-sm-4">Fecha de nacimiento</dt>
        <dd class="col-sm-8">@Model.FechaDeNacimiento.ToString("yyyy-MM")</dd>

        <dt class="col-sm-4">Edad</dt>
        <dd class="col-sm-8">@(DateTime.Now.Year - Model.FechaDeNacimiento.Year)</dd>
    </dl>

    <div class="d-flex gap-2">
        <a class="btn btn-outline-secondary swal-edit-link" asp-action="Edit" asp-route-id="@Model.Id">Editar</a>
        <a asp-action="Index" class="btn btn-primary ms-auto">Volver al listado</a>
        <form asp-action="Delete" method="post" asp-route-id="@Model.Id" class="swal-delete-form d-inline">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-danger">Eliminar</button>
        </form>
    </div>
</div>




