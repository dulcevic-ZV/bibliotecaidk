@model AppWebBiblioteca.Models.serviceCard

<div class="col-md-4 mb-3">
    <div class="card h-100 shadow-sm">
        <img src="@Model.ImageUrl" alt="Imagen" class="card-img-top" />
        <div class="card-body">
            <p class="card-text">@Model.Text</p>
        </div>
    </div>
</div>
